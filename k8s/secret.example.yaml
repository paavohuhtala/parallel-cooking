# Copy, set real values, and apply out of band — do not commit the filled-in version.
#   kubectl create secret generic parallel-cooking-auth \
#     --from-literal=BASIC_AUTH_USER=chef --from-literal=BASIC_AUTH_PASS='...'
apiVersion: v1
kind: Secret
metadata:
  name: parallel-cooking-auth
type: Opaque
stringData:
  BASIC_AUTH_USER: chef
  BASIC_AUTH_PASS: change-me
